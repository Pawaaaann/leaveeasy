rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all authenticated users to read and write data
    // This is for development - in production, implement proper role-based rules
    match /{document=**} {
      allow read, write: if true;
    }
  }
}